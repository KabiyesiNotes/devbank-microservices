services:
  backend:
    build: ./backend
    ports:
      - "3002:3000"
    environment:
      - PORT=3000

  frontend:
    build:
      context: ./frontend
      args:
        - REACT_APP_API_URL=http://localhost:3002
    ports:
      - "3001:80"
    depends_on:
      - backend
